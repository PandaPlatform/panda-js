var Panda = Panda || {};
(function ($) {
    $(document).one("ready", function () {
        Panda.init();
    });

    Panda = {
        init: function () {
            // Register library
        }
    };
})();
